import { Route } from '@angular/router';
import { RemoteEntry } from './entry';
import { Profile } from './Pages/features/profile/profile';

export const remoteRoutes: Route[] = [{ path: '', component: Profile }];
